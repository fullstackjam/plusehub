apiVersion: v1
kind: Service
metadata:
  name: {{ include "pulsehub.fullname" . }}
  labels:
    {{- include "pulsehub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "pulsehub.selectorLabels" . | nindent 4 }}
